# =============================================================================
# UNIVERSAL ALIASES - Work on all platforms
# =============================================================================

alias myhistory="history | awk '{CMD[\$2]++;count++;}END { for (a in CMD)print CMD[a] \" \" CMD[a]/count*100 \"% \" a;}' | grep -v \"./\" | column -c3 -s \" \" -t | sort -nr | nl |  head -n20"
alias ping8="ping 8.8.8.8"
alias v="nvim"
alias n="nvim"
alias valias="nvim ~/.alias && salias"
alias vrc="nvim ~/.config/nvim/init.lua"
alias salias="source ~/.alias"
alias cpav="cp -av"
alias rmrf="rm -rf"
alias topm="top -o MEM"
alias py="python"
alias py3="python3"
alias fuck='sudo $(fc -ln -1)' 
alias ducks='du -cks * | sort -rn | head'
alias duh='du -h'
alias dc='docker-compose'
alias dcb='docker-compose build'
alias dcbu='docker-compose build && docker-compose up'
alias gdh='git diff HEAD'
alias glf='git log --pretty=format: --name-only | sort | uniq -c | sort -rg | head -10'
alias glr='git pull --rebase'
alias glol1='git log -1 --format="%H"'
alias gaas="gaa & gst"
alias ga="git add"
alias gaa="git add ."
alias gdc="git diff --cached"
alias grh='git reset HEAD'
alias gst="git status"
alias gap="git add -p"
alias gco="git checkout"
alias gd="git diff"
alias gp="git push"
alias gpf="git push --force-with-lease"
alias gcmsg="git commit --message"
alias grebasemaster="gco master && glr && gco - && git rebase master"
alias grebasemain="gco main && glr && gco - && git rebase main"
alias grebasedevelop="gco develop && glr && gco - && git rebase develop"
alias gmergemaster="gco master && glr && gco - && git merge master"
alias gmergedevelop="gco develop && glr && gco - && git merge develop"
alias gmergemain="gco main && glr && gco - && git merge main"
alias grv="git remote -v"
alias gcb="git checkout -b"
alias gcl="git clone"
alias wifi='networksetup -setairportpower en0 off && networksetup -setairportpower en0 on'
alias findn='find . -name'
alias vinit='v ~/.config/nvim/init.lua'
alias psa='ps aux | grep'
#alias e='emacsclient "$@" -a "" -c'
#alias ec='emacsclient "$@" -a "" -c -n'
alias f=fzf
alias vf='v $(f)'
alias nf='n $(f)'
alias vn='v ~/notas'
alias ocv="cd ~/notas && opencode"

alias l='ls -lah'


alias ag="ag --hidden"
alias envg="env | grep "

# Claude

#tmux
alias tn='tmux new-session -A -s main'

# Dotfiles
alias dotfiles='cd ~/dotfiles'


# npm
alias npmclean="rm -rf node_modules && npm install"
alias nr="npm run"
alias ns="npm start"
alias ni="npm install"
alias nis="npm install --save"
alias nisd="npm install --save-dev"
alias nig="npm install -g"

# =============================================================================
# LINUX-SPECIFIC ALIASES - For Arch Linux and other Linux distros
# =============================================================================

alias ys="yay -S"
alias arch-drop-caches='sudo paccache -rk3; yay -Sc --aur --noconfirm'
alias arch_update_all='export TMPFILE="$(mktemp)"; \
    sudo true; \
    rate-mirrors --save=$TMPFILE arch --max-delay=21600 \
      && sudo mv /etc/pacman.d/mirrorlist /etc/pacman.d/mirrorlist-backup \
      && sudo mv $TMPFILE /etc/pacman.d/mirrorlist \
      && arch-drop-caches \
      && yay -Syyu --noconfirm'

# Linux system configurations
# (Window manager configs can be added here if needed)

# =============================================================================
# MACOS-SPECIFIC ALIASES - For macOS systems
# =============================================================================

# Homebrew shortcuts (macOS)
# Note: Fish shell will ignore bash-style conditionals, but these aliases will work on macOS
alias brewup="brew update && brew upgrade"
alias brewclean="brew cleanup"
